# Follow-up Items

Items noted during implementation that are out of scope for this PRD but worth tracking:

- `next-themes` is installed but unused — remove in a future cleanup pass (noted in CHANGES.md)
- `input.tsx` and `textarea.tsx` shadcn primitives are unused — can be deleted if not planned for use
- The `assets/screenshot.png` is an AI-generated dashboard example — replace with a real product screenshot when available
